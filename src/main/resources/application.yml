server:
  port: 8000

spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create-drop

  datasource:
    url: jdbc:mysql://localhost:3306/watchers?useSSL=false&serverTimezone=UTC
    username: watchers_admin
    password: watchers_admin
    driver-class-name: com.mysql.jdbc.Driver

#logging:
#  level:
#    org:
#      springframework:
#        web: DEBUG
#      hibernate: DEBUG
